$(document).ready(function() {
$("#triangle-form").submit(function(event) {
  event.preventDefault();
  const side1 = parseInt($("#side1").val());
  const side2 = parsetInt($("#side2").val());
  const side3 = parseInt($("#side3").val());
  
  });
});